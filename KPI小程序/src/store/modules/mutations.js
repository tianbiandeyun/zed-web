export default {
  GETVIDEOINFO(state, res) {
    state.getVideoInfo = res;
  },
  GETADDRESSMULTI(state, res) {
    state.getAddressMulti = res;
  },
  GETADDRESS(state, res) {
    state.getAddress = res;
  },
  REGISTUSER(state, res) {
    state.registUser = res;
  }
};
